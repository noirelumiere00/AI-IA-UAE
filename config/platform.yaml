# プラットフォーム設定（プロバイダ・モデル階層）。秘密情報は書かない（env/Secrets Manager）。
default_profile: heuristic   # heuristic（既定・無料/オフライン） / bedrock / api
profiles:
  bedrock:
    name: bedrock
    region: ap-northeast-1
models:   # 全工程 Haiku（コスト/速度優先・ユーザー指定）
  classify: claude-haiku-4-5
  summarize: claude-haiku-4-5
  draft: claude-haiku-4-5
